# -*- coding: utf-8 -*-
"""Data_Secondary_Benchmark.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1r9Etis4FU03ftJSsuHlc0AVF1xibqS3C

Install and Import Libraries
"""

!pip install transformers torch nltk seaborn scikit-learn tqdm --quiet

import pandas as pd
import numpy as np
import re
import torch
from tqdm import tqdm
import seaborn as sns
import matplotlib.pyplot as plt

from sklearn.metrics import accuracy_score, f1_score, confusion_matrix
from nltk.sentiment import SentimentIntensityAnalyzer
import nltk

from transformers import AutoTokenizer, AutoModelForSequenceClassification

"""Load Dataset"""

from google.colab import drive
drive.mount('/content/drive')

df = pd.read_csv(
    "/content/drive/MyDrive/NLP_Project/Datasets/WSB_full.csv",
    header=None,
    names=["label", "text"]
)

# Remove accidental header row if included
df = df[df["label"] != "label"]

# Convert to numeric
df["label"] = pd.to_numeric(df["label"], errors="coerce")

# Drop rows where conversion failed
df = df.dropna(subset=["label"])

# Convert to int
df["label"] = df["label"].astype(int)

# Map to -1,0,1 scale
label_map = {0: 0, 1: 1, 2: -1}
df["true_label"] = df["label"].map(label_map)

print("Label dtype:", df["true_label"].dtype)
print("Unique labels:", df["true_label"].unique())

print("Rows:", len(df))
df.head()

"""Clean Text"""

def clean_text(text):
    text = str(text).lower()
    text = re.sub(r"http\S+", "", text)
    text = re.sub(r"@\w+", "", text)
    text = re.sub(r"[^a-z\s]", "", text)
    return text.strip()

df["clean_text"] = df["text"].apply(clean_text)

"""VADER Model"""

import nltk
from nltk.sentiment import SentimentIntensityAnalyzer

nltk.download("vader_lexicon")

sia = SentimentIntensityAnalyzer()

def vader_predict(text):
    score = sia.polarity_scores(str(text))["compound"]
    if score > 0.05:
        return 1
    elif score < -0.05:
        return -1
    return 0

print("Running VADER...")
df["vader_pred"] = df["clean_text"].apply(vader_predict)

print(df["vader_pred"].value_counts())

"""FINBERT Model"""

from transformers import AutoTokenizer, AutoModelForSequenceClassification
import torch
from tqdm.notebook import tqdm

device = torch.device("cuda" if torch.cuda.is_available() else "cpu")

tokenizer = AutoTokenizer.from_pretrained("ProsusAI/finbert")
model = AutoModelForSequenceClassification.from_pretrained("ProsusAI/finbert")

model.to(device)
model.eval()

def finbert_predict(text):

    inputs = tokenizer(
        str(text),
        return_tensors="pt",
        truncation=True,
        padding=True,
        max_length=512
    ).to(device)

    with torch.no_grad():
        outputs = model(**inputs)
        probs = torch.softmax(outputs.logits, dim=1)

    label = torch.argmax(probs, dim=1).item()

    # FinBERT mapping
    # 0 = negative
    # 1 = neutral
    # 2 = positive

    if label == 0:
        return -1
    elif label == 1:
        return 0
    else:
        return 1


print("Running FinBERT...")

df["finbert_pred"] = [
    finbert_predict(t) for t in tqdm(df["clean_text"], desc="FinBERT Predictions")
]

"""Recrate Label Mapping"""

label_map = {
    0: 0,   # neutral
    1: 1,   # positive
    2: -1   # negative
}

df["true_label"] = df["label"].map(label_map)

print(df["true_label"].value_counts())

"""Accuracy Benchmark"""

from sklearn.metrics import accuracy_score, f1_score, confusion_matrix

print("\nACCURACY BENCHMARK")

for model_name in ["vader_pred", "finbert_pred"]:

    temp = df[["true_label", model_name]].dropna()

    acc = accuracy_score(temp["true_label"], temp[model_name])
    f1 = f1_score(temp["true_label"], temp[model_name], average="macro")

    print(f"\nModel: {model_name}")
    print("Accuracy:", acc)
    print("Macro F1:", f1)
    print("Confusion Matrix:")
    print(confusion_matrix(temp["true_label"], temp[model_name]))

"""Adversial Logic Flip"""

logic_flip_map = {
    "beat": "missed",
    "beats": "misses",
    "exceeded": "fell short of",
    "profit": "loss",
    "gains": "losses",
    "rise": "fall",
    "rises": "falls",
    "up": "down",
    "strong": "weak",
    "growth": "decline"
}

def logic_flip(text):
    flipped = text
    for word, replacement in logic_flip_map.items():
        flipped = flipped.replace(word, replacement)
    return flipped

df["flipped_text"] = df["clean_text"].apply(logic_flip)

"""Run Models on Flipped Text"""

df["vader_flip"] = df["flipped_text"].apply(vader_predict)
df["finbert_flip"] = [finbert_predict(t) for t in tqdm(df["flipped_text"], desc="FinBERT Flipped")]

"""Logic Robustness"""

def logic_robustness(original, flipped):
    changed = (original != flipped).sum()
    return changed / len(original)

vader_lrs = logic_robustness(df["vader_pred"], df["vader_flip"])
finbert_lrs = logic_robustness(df["finbert_pred"], df["finbert_flip"])

print("VADER LRS:", vader_lrs)
print("FinBERT LRS:", finbert_lrs)
print("Logic Gap (FinBERT - VADER):", finbert_lrs - vader_lrs)

"""Export Failure Cases"""

# A failure occurs when the model prediction DOES NOT change
# after the logic flip

df["vader_fail"] = df["vader_pred"] == df["vader_flip"]
df["finbert_fail"] = df["finbert_pred"] == df["finbert_flip"]

vader_fail = df[df["vader_fail"]]
finbert_fail = df[df["finbert_fail"]]

vader_fail.to_csv("vader_logic_failures.csv", index=False)
finbert_fail.to_csv("finbert_logic_failures.csv", index=False)

print("Failure CSVs exported.")

#Downlaod error files
from google.colab import files
files.download("vader_logic_failures.csv")
files.download("finbert_logic_failures.csv")

"""Plot: Sentiment Signal vs True Score"""

sns.set(style="whitegrid")

comparison = df[["true_label", "vader_pred", "finbert_pred"]]
grouped = comparison.groupby("true_label").mean()

plt.figure(figsize=(8,5))
grouped.plot(kind="bar")

plt.title("Average Model Predictions by True Sentiment")
plt.xlabel("True Sentiment (-1=Neg, 0=Neutral, 1=Pos)")
plt.ylabel("Average Predicted Score")
plt.xticks(rotation=0)
plt.legend(["VADER", "FinBERT"])
plt.tight_layout()
plt.show()

